import React from 'react';
import Timeline from "../../../Timeline";

const MapView = (props) => {
    return (
        <div>
            <Timeline/>
        </div>
    );
};

export default MapView;
